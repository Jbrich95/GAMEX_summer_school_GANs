# Extremal GAN notebooks

Edinburgh Summer School on Generative AI for Extremes

Requirements:
- Python 3.10+
- torch
- numpy
- matplotlib

Run:
```
python3 -m venv gan_ext_env
source gan_ext_env/bin/activate
pip install torch numpy matplotlib scipy statsmodels jupyter
```

Then launch Jupyter lab
```
jupyter lab
```
